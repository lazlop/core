refresh_price_data:
  name: Refresh Price Data
  description: Manually refresh the price data from the CalFlexHub API.
  target:
    entity:
      integration: oadr_test
      domain: sensor
  fields:
    entity_id:
      name: Entity
      description: The CFH Price sensor entity to refresh.
      required: true
      selector:
        entity:
          integration: oadr_test
          domain: sensor